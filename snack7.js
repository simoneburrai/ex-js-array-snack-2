const books = require("./data")


// - ✅ Usa `.reduce()` per creare `tagCounts`,
//  un oggetto che conta quante volte ogni tag è usato tra tutti i libri.

console.log(books);

const tagCounts = books.reduce((acc, val)=>{
    
}, {})


//PROBLEMI NELLA RISOLUZIONE, DA RIVEDERE